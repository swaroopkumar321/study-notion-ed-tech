# StudyNotion

Test accounts (use these to sign in):

- instructor@test.com — instructor123
- student@test.com — student123 


A small full-stack edtech app (React frontend, Node/Express backend, MongoDB).

Quick start:

```bash
npm install
cd server && npm install
npm run dev
```

Environment:
- Create `server/.env` with `MONGODB_URL` set to your MongoDB connection string.


